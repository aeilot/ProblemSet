#include <cstdio>
#include <iostream>

using namespace std;

int main() {
    // Type your code here
    long long t, l, r, x, a, b;
    cin >> t;
    while (t--) {
        cin >> l >> r >> x >> a >> b;
        
    }
    return 0;
}